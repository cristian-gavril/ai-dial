{{- if .Values.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dial-core.fullname" . }}
  labels:
    {{- include "dial-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dial-core.selectorLabels" . | nindent 4 }}
{{- end -}}
